[project]
name = "verbox-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pranav Jain", email = "pranav.avlok@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv-python>=0.0.1",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
]

[project.scripts]
verbox-dashboard = "verbox_dashboard:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
